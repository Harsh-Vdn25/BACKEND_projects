services:
  blogapi:
    build: .
    ports:
     - "5001:5001"
    env_file:
      - .env
    environment:
      MONGO_URL: "mongodb://db:27017/blogapi"
    depends_on:
      - db
  db:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
volumes:
  mongodata: